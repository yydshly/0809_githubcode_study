# Reserved sealed defect holdout partition

This partition is intentionally empty. Future contents must be frozen before
QA acceptance runs and must not reuse source families or capture sessions from
other partitions.

