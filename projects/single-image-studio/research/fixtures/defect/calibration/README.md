# Reserved defect calibration partition

This partition is intentionally empty. It is reserved for explicitly derived,
rights-traceable defect fixtures used to calibrate QA behavior. It must not be
used as sealed acceptance evidence.

