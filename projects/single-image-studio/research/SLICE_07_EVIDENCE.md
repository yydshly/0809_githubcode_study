# Slice 07 definition-freeze evidence

Slice 07 froze the results-zero definition for
`REG-NORM-SHARP-CANONICAL-PNG@0.7.0` at
`2026-08-15T12:15:57.873Z`. This is a research admission baseline, not a
Gate-B result or product-support claim. The real Sharp image worker has not
been started in Slice 07.

## Frozen tree

- definition index content hash: `298e2ee08c5471eb50f95aff26e46d3459e26bb586ef8c4d59cb01b96072b2d6`;
- definition index file SHA-256: `de2eabd3bb5794494723a8123753a425fb5ee97c0d2a014b58698ef6572bd645`;
- generator SHA-256: `c42837da0347980f468e4c26107c94242e76ac0751218ed15014d6e358659f1d`;
- schema-tree SHA-256: `944a7e6eae34188d6d84492871d29edcdf9de3f55f28027130ef88436cd811f8`;
- 39-file descendant-tree SHA-256: `6c94fd4cb707355eeea78c188733c18380a65a3c887ea6e185da0b2eed6cea75`;
- full 41-file tree SHA-256: `bc608ed9c7aa61a3165a35018149acbf2800035b769fb7fd379d6e6847fa0481`;
- pinned README SHA-256: `94d1269e820ab45b773283782b8c198cd1d0285b0b934db113667e5a4563b5f6`.

The tree contains 16 strict schemas, 23 non-index records, one index, two
operation manifests, and 12 lineage-only source wrappers. It freezes 12
public project-original synthetic sources and 36 planned attempts: normalize
and export each use three applicable plus three rejection sources, repeated
three times. It copies no image bytes and contains no `results` tree.

## Admission and implementation boundary

The definition pins a fresh local runtime attestation, named hardware without
host identifiers, the candidate and two operation contracts, rights, plan,
preregistrations, manifests, and seven implementation files. The registered
driver admits only a clean pushed baseline, one invocation, normalize then
export, zero replacements, and the exact `results/open-smoke` root. All six
rejection sentinels were exercised with a fake executor and produced their
frozen stable error before any worker start.

The central validator independently checks schema vocabulary and closure,
record self-hashes and references, actual Slice 05 source/gold bytes, decoded
pixel hashes for applicable sources, implementation hashes, runtime drift,
denominators, results absence, full-tree pins, and byte-for-byte regeneration.
Its production report is `valid=true`, `issues=[]`, with pins, runtime recheck,
and regeneration all verified.

## Verification

- six Slice 07 suites: `38 / 38` pass;
- complete `npm.cmd run verify`: `363 / 363` tests pass, followed by syntax
  checks with exit code 0;
- explicit `node --check` covers the new generator, driver, validator, worker,
  adapter and runner sources;
- `git diff --check` and local-link checks are required again immediately
  before commit.

Tests use only in-memory synthetic bytes, fake child processes and
system-temporary trees. They do not invoke the registered smoke or the real
Sharp image path.

## Evidence boundary and next action

`C1/U1/E1/R1/O1/G1/V1` remain `0`; Release Gate remains `none / 0 / 0`;
`productSupport=false`; calibration, formal holdout, real-user photos, model
weights, UI and server integration remain absent. This exact definition was
committed and pushed as `d056390`; its registered smoke subsequently closed
without Gate-B authorization. See
[SLICE_07_RESULT_EVIDENCE.md](SLICE_07_RESULT_EVIDENCE.md). Slice 07 must not be
rerun or rewritten; a later version is required for corrected evaluation.
