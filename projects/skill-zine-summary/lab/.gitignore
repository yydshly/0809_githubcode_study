# Local upstream checkouts. These may contain nested .git directories and
# third-party material that must not be committed into this study repository.
sources/

# Generated or rights-sensitive experiment artifacts.
outputs/
cache/
inputs/private/

# Local image-generation outputs used by the comparison site. The gallery code
# is tracked, but generated images stay local until every upstream license and
# publication right is cleared.
web/public/generated/
