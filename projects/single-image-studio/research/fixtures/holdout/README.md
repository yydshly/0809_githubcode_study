# Reserved holdout partition

This partition is intentionally empty. Populate it only after the evaluation
contract is frozen, with source families and capture sessions that do not occur
in `dev/calibration`. Adding files without a FixtureManifest is invalid.

