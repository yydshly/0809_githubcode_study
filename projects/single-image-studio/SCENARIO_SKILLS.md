# 场景技能层设计合同

## 设计合同

```text
Entry mode: revision-led
Request revision: 2
Target user and context: 已上传一张图片、知道想得到什么用途，但不理解裁剪、Alpha、画布和导出参数的普通用户
Desired first impression: 先看到“我要做什么”，随后仍能检查和微调每一步
Visual ambition: Functional
Experience architecture: Editorial Flow
Visual constraints: 沿用现有任务卡、工作预览、设置卡和结果工作台；场景优先于自由工具，不新增装饰性页面
Information constraints: 明确本地/远程、是否依赖服务、会改变什么、输出什么；不得声称证件合规或 AI 自动质量判断
Operation constraints: 复用现有 EditState、renderer、BackgroundRemovalProvider、蒙版修正、下载合同与既有 OpenAI image edit route；老照片任务使用独立 ID 和服务端固定提示词白名单，不新增模型或依赖
State constraints: 服务未配置时远程场景必须不可选；失败不覆盖来源、不自动重试；换处理方向保留当前图片并退休旧结果
Environment constraints: 当前首轮桌面 Chromium；移动与正式发布不在本次范围
Primary journey: 上传并确认图片 → 选择实际用途 → 自动载入场景起点 → 用户微调 → 生成、检查和下载
User-defined phases: 场景技能框架；商品白底图；报名照/底色头像；社交头像与封面；老照片温和修复实验；工程与浏览器验收
Required artifacts: scenario-skills.js、场景优先任务选择、四条任务设置与结果合同、老照片固定提示词合同、测试、状态文档
Autonomy authorization: 用户已明确“确定”，授权在既有产品边界内直接实现
User-decision boundary: 新外部服务、机构证件合规承诺、真实计费老照片调用、批量/账号/正式发布不在本次授权内
Observable completion criteria: 四个场景以用户用途出现；场景默认参数进入各自 renderer/provider；老照片任务必须明确生成风险且服务端拒绝任意提示词；商品白底默认导出不透明 JPEG；无服务时远程场景关闭；自由工具仍可用
```

## 场景与边界

| 场景技能 | 复用路径 | 默认起点 | 当前承诺 |
| --- | --- | --- | --- |
| 商品白底图 | 本地构图 → 远程抠图 → 本地修边/白底 | 1:1、最长边 1600、白底 JPEG | 制作普通商品展示图；不做平台合规、阴影或批量 |
| 报名照 / 底色头像 | 本地头肩构图 → 远程抠图 → 本地修边/选底 | 1:1、人物位置略上移、白底起点 | 普通报名和头像用途；不承诺护照、签证或机构受理 |
| 社交头像与封面 | 完全本地的比例/尺寸模板 | 1:1、最长边 1080 | 提供透明构图起点；不冒充平台永久规范 |
| 老照片温和修复 | 既有 OpenAI image edit route → 固定修复提示词 → 原图/副本比较 | 克制修复、人物身份特征优先 | 生成便于观看的修复副本；不承诺无损、档案级、身份或历史细节准确 |

老照片场景现在只完成工程入口，不代表质量已通过。它明确复用生成式图片编辑，不提供上色开关、档案复原、法证恢复或自动真伪判断。若真实夹具显示人物、文字或物件漂移不可接受，应关闭该实验，而不是放宽文案。

## 覆盖清单

| 用户阶段 | 表面 / 状态 | 需要的证据 | Owning stage | 状态 | 下一动作 |
| --- | --- | --- | --- | --- | --- |
| 场景技能框架 | 任务选择页，服务可用/不可用 | 注册表、分组与浏览器 DOM 顺序 | 3–4 | pass | `SCENARIO_SKILLS` 精确 4 项；浏览器 run `cae0c93e-0c47-42c4-a744-5fcf8419868f` 核对场景 / 工具 / 创意顺序 |
| 商品白底图 | 设置、远程确认、结果、下载 | 状态、初始设置、恢复与下载合同测试 | 5–6 | pass | 1:1 / 1600 起点、白底修正起点与 opaque-JPEG 下载均由自动化绑定；未发起新的远程调用 |
| 报名照 / 底色头像 | 设置、结果文案、合规边界 | 状态、文案、恢复与下载测试 | 5–6 | pass | 1:1 / 4:5、人物上移起点、白底起点与机构免责声明均已绑定 |
| 社交头像与封面 | 本地设置与模板 | renderer、模板测试与真实浏览器生成 | 5–6 | pass | `desktop-common` 真实生成并重开 1440 × 810 PNG |
| 老照片温和修复 | 设置、风险确认、服务端合同、结果/下载 | 6 种固定 prompt、任意 prompt 拒绝、禁用状态与下载命名 | 5–6 | engineering-pass | 未调用真实模型；人物 / 文字 / 历史细节质量保持 unknown |
| 两档桌面任务卡 | 1280 × 720、1440 × 900 | 浏览器 DOM、无横向溢出与两条本地旅程 | 7 | pass | 新场景顺序下 2/2 浏览器旅程通过 |
| 场景视觉细节 | 卡片色彩、层级和真实指针观感 | 可见截图与人工观察 | 7 | defer | 已尝试发现应用内浏览器控制 / 截图工具，但本任务只暴露打开页面能力；当前由 DOM、CSS 与无溢出证据兜底。应用内浏览器截图控制再次可用时复核两档视口 |
| 能力关闭 | 未配置远程服务 | 状态测试和 fallback | 6–8 | pass | 商品 / 报名场景随 Provider 状态关闭，三个本地工具保持可用 |
| 远程场景真实提交 | 商品 / 报名照各一次端到端 | Provider 调用、结果与下载观察 | 7–8 | defer | 本次安全浏览器路线明确不调用远程服务；取得一次有预算边界的场景调用授权时，以项目原创图片分别复核，不重用用户照片 |
| 工程交付 | 产品测试、语法、文档 | `verify:product`、浏览器报告、diff / link 检查 | 9 | pass | 产品测试 190/190；196 个 JavaScript 文件语法通过；浏览器本地旅程 2/2 |
